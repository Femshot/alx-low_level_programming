#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point: prints all posible combinations of two two-digit numbers
 * Return: 0 always
 */

int main(void)
{
	int a, b, c, d;

	for(a = '0', a <= '9', a++);
	{
		for 
